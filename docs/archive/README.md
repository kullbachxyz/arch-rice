# Archived docs

These setups are no longer used. SSH and GPG keys are now managed through
KeePassXC (built-in SSH agent + secret storage), which replaced the
`pam_ssh` / `pam-gnupg` PAM-based auto-unlock at TTY login.

Kept for reference only.
